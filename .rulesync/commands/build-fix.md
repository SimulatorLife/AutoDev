---
targets: ["*"]
description: Fix outstanding build issues, failures, or errors in the project fully and verify a clean build.
---

Fix the outstanding build issues/failures/errors in the project fully and properly. Ensure that all dependencies are correctly installed, configurations are set up, and any code errors are resolved. Run the build process to verify that it completes successfully without any errors or warnings. Explain any and all changes made to the codebase or configuration files.
