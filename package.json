{
  "name": "@simulatorlife/autodev",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/**/*.test.mjs scripts/codex-model-router.test.mjs",
    "validate:weights": "node tests/weights.test.mjs",
    "test:python": "python3 -m unittest discover -s tests -p 'test_*.py'",
    "validate:actionlint": "actionlint",
    "validate:shell": "find scripts -type f -name '*.sh' -exec shellcheck --severity=warning {} +"
  },
  "packageManager": "pnpm@10.32.1",
  "devDependencies": {
    "@playwright/mcp": "^0.0.80",
    "lsp-mcp-server": "^1.1.20",
    "rulesync": "16.30.2",
    "typescript": "^5.9.3",
    "typescript-language-server": "^5.3.0"
  }
}
